import { HesitateDirective } from './hesitate.directive';

describe('HesitateDirective', () => {
  it('should create an instance', () => {
    const directive = new HesitateDirective();
    expect(directive).toBeTruthy();
  });
});
