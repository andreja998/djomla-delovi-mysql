import { DrahDropUploadDirective } from './drah-drop-upload.directive';

describe('DrahDropUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DrahDropUploadDirective();
    expect(directive).toBeTruthy();
  });
});
